# Official Noctalia plugins catalog.
# Index of every plugin this source ships — the minimum needed to render, search,
# and compat-check the list. The per-plugin plugin.toml stays authoritative; the
# host re-reads it on enable. Keep one [[plugin]] row per plugin subdirectory.

[[plugin]]
id = "noctalia/mechsound"
name = "Mechanical Sounds"
version = "1.0.0"
author = "cleboost"
license = "MIT"
icon = "volume-up"
description = "Simulates keyboard typing sounds with customizable mechanical switch soundpacks."
min_noctalia = "5.0.0"
tags = ["utility", "audio", "keyboard"]

[[plugin]]
id = "cleboost/test-plugin"
name = "Test Plugin"
version = "1.0.0"
author = "cleboost"
license = "MIT"
icon = "flask"
description = "Plugin de test pour vérifier que tout fonctionne."
min_noctalia = "5.0.0"
tags = ["test", "demo"]

[[plugin]]
id = "cleboost/zed-projects"
name = "Zed Projects"
version = "1.0.0"
author = "cleboost"
license = "MIT"
icon = "folder-open"
description = "Open a recent Zed project from the launcher. Type /zed to list your projects."
min_noctalia = "5.0.0"
tags = ["zed", "launcher", "projects"]
